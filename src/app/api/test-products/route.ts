import { NextResponse } from 'next/server'; import { ProductService } from '../../../backend/services/product.service'; export const dynamic = 'force-dynamic'; export async function GET() { try { const p = new ProductService(); const data = await p.getProducts('all', { branch: 'Pusat' } as any); return NextResponse.json(data); } catch(e: any) { return NextResponse.json({error: e.message}, {status: 500}); } }
